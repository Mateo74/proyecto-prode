{
  "name": "prodefootball",
  "version": "1.0.0",
  "description": "App de predicciones de partidos de futbol",
  "private": true,
  "scripts": {
    "test": "npm run lint:js && npm run test:frontend",
    "test:all": "npm run lint:js && npm run test:frontend && npm --prefix prode-backend test",
    "test:frontend": "playwright test",
    "lint:js": "node --check js/app.js js/api.js js/i18n.js js/predictions.js js/knockout.js js/config.example.js",
    "sync": "cap sync",
    "android": "cap run android",
    "ios": "cap run ios",
    "open:android": "cap open android",
    "open:ios": "cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@playwright/test": "^1.60.0",
    "http-server": "^14.1.1"
  }
}
